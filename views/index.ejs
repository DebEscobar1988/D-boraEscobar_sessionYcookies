<!DOCTYPE html>
<html lang="en">
<%- include('partials/head',{
    title : 'Formulario de Usuarios'
}) %>



<body class="<%= locals.userData && locals.userData.color %>">

<main class="registro">
    <h1>Bienvenid@!!!</h1>

   
    
 <% if (locals.userData) { %>

        <p class="frase">Hola <b> <%= userData.name %></b>. <br> 
            Elegiste el color: <b> <%= userData.color  %></b>. <br> 
            Tu email es: <b><%= userData.email %></b>. <br> 
            Tu edad es: <b><%= userData.age %></b> años
         </p>
         <a href="/profile">PERFIL</a>
         <a href="/logout">Cerrar Cesión</a>
             
<% }else{  %>
    
    <form method="post">
        <div>
       <label for="name">Nombre:</label>
        <input type="text" name="name">
<small><%= locals.errors && errors.name ? errors.name.msg : null %></small>
        </div>
        <div>
            <select name="color" >
                <option value="">Elije un color</option>
                <option value="Rojo">Rojo</option>
                <option value="Verde">Verde</option>
                <option value="Azul">Azul</option>
                <option value="Violeta">Violeta</option>
            </select>
        </div>
        <div>
            <label for="email">Email:</label>
             <input type="email" name="email" value="<%= locals.old && old.email ? old.email: "" %>"> 
         </div>
        <div>
        <label for="age">Edad:</label>
        <input type="number" name="age"> 
         </div>
<div>
<label  for="remember">Recordame <input id="remember" type="checkbox" name="remember"></label>

</div>
        <button type="submit">ENVIAR</button>
      
        </form>
        
   
         <% } %>
</main>

<%- include('partials/footer') %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.min.js" integrity="sha384-Rx+T1VzGupg4BHQYs2gCW9It+akI2MM/mndMCy36UVfodzcJcF0GGLxZIzObiEfa" crossorigin="anonymous"></script>
</body>

</html>
